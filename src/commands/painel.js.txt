import {
  SlashCommandBuilder,
  EmbedBuilder,
  ActionRowBuilder,
  ButtonBuilder,
  ButtonStyle
} from "discord.js";

export const data = new SlashCommandBuilder()
  .setName("painel")
  .setDescription("Abrir painel da facção");

export async function execute(interaction, client) {
  try {
    const embed = new EmbedBuilder()
      .setTitle("🟣 Painel da Facção NINE")
      .setDescription(
        "Selecione uma opção abaixo:\n\n" +
        "📌 **Setar-se na facção**\n" +
        "🎯 **Criar ação**\n" +
        "⭐ **Ticket Elite**"
      )
      .setColor("#6a0dad");

    const row = new ActionRowBuilder().addComponents(
      new ButtonBuilder()
        .setCustomId("painel_setin")
        .setLabel("Pedir Set")
        .setStyle(ButtonStyle.Primary),

      new ButtonBuilder()
        .setCustomId("painel_acao")
        .setLabel("Criar Ação")
        .setStyle(ButtonStyle.Secondary),

      new ButtonBuilder()
        .setCustomId("painel_elite")
        .setLabel("Ticket Elite")
        .setStyle(ButtonStyle.Success)
    );

    await interaction.reply({
      embeds: [embed],
      components: [row],
      ephemeral: true
    });

  } catch (err) {
    console.error("ERRO NO /painel:", err);
    await interaction.reply({
      content: "❌ Erro interno no painel.",
      ephemeral: true
    });
  }
}
